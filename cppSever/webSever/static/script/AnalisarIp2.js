const AnalisarIp2 = (array_ip,ip) => {

    for( const obj_ip of array_ip) {
        
    if (obj_ip.ip === ip) {
        console.log("O IP está registrado no banco");
        return obj_ip;
        break;
    } else {
        
    }

}

}